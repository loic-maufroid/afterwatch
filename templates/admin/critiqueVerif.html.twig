{% extends "baseAdmin.html.twig" %}

{% block title %}{{ parent() }} - Vérification avant publication{% endblock %}

{% block body %}
{{ review.titre }}
{{ review.contenu }}

<a href="{{ path('critique_publier',{id: review.id}) }}"><button class="btn btn-success">Publier</button></a>
<a href="{{ path('admin_critiqueslist',{page: 1}) }}"><button class="btn btn-danger">Ne pas publier</button></a>
{% endblock %}